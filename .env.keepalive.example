# Example only. Copy outside the repository to /etc/healinghi/keepalive.env.
# Restrict the real file to its owner (chmod 600). Do not use a service-role key.
SUPABASE_URL='https://yourprojectref.supabase.co'
SUPABASE_ANON_KEY='your_anon_or_publishable_key'
# Use a DIFFERENT check from the GitHub Actions monitor.
KEEPALIVE_HEARTBEAT_URL='https://hc-ping.com/your-backup-check-uuid'
